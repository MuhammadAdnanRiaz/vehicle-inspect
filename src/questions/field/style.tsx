import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    width: '100%',
    justifyContent: 'flex-start',
  },
  input: {
    height: 40,
    marginVertical: 12,
    borderWidth: 1,
  },
});
